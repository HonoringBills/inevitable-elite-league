{
  "name": "inevitable-elite-league",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy:cloudflare": "npm run build && npx wrangler@4.120.0 deploy",
    "preview:cloudflare": "npm run build && npx wrangler@4.120.0 versions upload"
  }
}
